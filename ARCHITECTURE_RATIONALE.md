# Architecture Rationale

이 문서는 [ARCHITECTURE.md](./ARCHITECTURE.md)의 규칙을 선택한 이유를 설명합니다. 실제 구조와 명령은 `ARCHITECTURE.md`를 기준으로 합니다.

## 얕은 계층을 유지하는 이유

현재 규모에서는 Repository, UseCase, Controller 같은 추가 계층이 코드 탐색 비용을 늘리는 반면 얻는 이점이 작습니다. 컴포넌트, 오케스트레이션 훅, 순수 도메인 로직, 외부 I/O 서비스의 얕은 구조로도 책임과 테스트 경계를 분명하게 나눌 수 있습니다.

새 계층은 반복되는 실제 문제가 확인되고 테스트 가능성이나 안전성이 분명히 개선될 때만 도입합니다.

## 트랜잭션 검증이 서비스 가까이에 있는 이유

자동배정과 면접 완료는 최신 Firestore 문서를 다시 읽은 뒤 충돌, revision, 지원 상태를 동시에 검증해야 합니다. 기본 계산은 순수 도메인 함수가 담당하지만, 동시성 상황의 최종 검증은 I/O와 분리할 수 없습니다.

따라서 서비스는 알고리즘을 소유하지 않되 원자적 저장을 보장하는 마지막 불변조건 검사를 담당합니다.

## 보안 규칙 테스트를 별도로 실행하는 이유

클라이언트 단위 테스트만으로는 공개 지원자 token과 로그인 운영진의 접근 경계를 증명할 수 없습니다. Firestore Emulator에서 실제 Rules를 적용한 테스트를 CI의 독립 단계로 실행합니다.

## 이력을 덮어쓰지 않는 이유

면접 일정 초기화, 지원 철회, 완료 취소는 현재 상태를 바꾸지만 과거 배정과 기록을 삭제하지 않습니다. 운영 중 문제를 추적하고 V4 전체 내보내기·신입부원 전환에 활용하려면 변경 이벤트와 스냅샷을 보존해야 합니다.
